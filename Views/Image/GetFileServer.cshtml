@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

<div>
    @foreach(var file in ViewBag.images) //foreach and get all files
    {
        <img src="~/images/@Model.Path.GetFileName(file)" />
    }
</div>